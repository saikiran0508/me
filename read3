import shutil

# Define the local file path
local_file_path = 'local_file.txt'

# Define the network folder path
network_folder_path = r'\\server\folder'

# Upload the file to the network folder
shutil.copy(local_file_path, network_folder_path)

